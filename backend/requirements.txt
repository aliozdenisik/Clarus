# Core dependencies
# SECURITY: CVE-2026-0994 - protobuf DoS via json_format.ParseDict()
# Status: No patch available (affects <=6.33.4, latest=6.33.4 still vulnerable)
# Risk: LOW - Not exploitable (protobuf used only for internal gRPC/ONNX, no user-controlled JSON parsing)
# Track: https://github.com/protocolbuffers/protobuf/pull/25239
# SECURITY: ecdsa Minerva timing attack (CVE pending) - no patch, risk LOW (PyJWT transitive dep, not directly used)
qdrant-client>=1.7.0
grpcio>=1.59.0
fastembed>=0.2.0
requests>=2.28.0
aiohttp>=3.8.0
tqdm>=4.65.0
rich>=13.0.0
python-dotenv>=1.0.0

# Data processing
pandas>=2.0.0
pyarrow>=14.0.0
diskcache>=5.6.0
tenacity>=8.2.0
pybreaker>=1.0.0

# Turkish lemmatization (for BM25)
zeyrek>=0.1.0

# Arabic NLP (morphological search)
tashaphyne>=0.3.0
pyarabic>=0.6.0

# Web Application (FastAPI)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sse-starlette>=2.0.0
pydantic-settings>=2.1.0
sentry-sdk[fastapi]

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0

# Authentication
PyJWT[crypto]>=2.8.0
bcrypt>=4.0.0
python-multipart>=0.0.22
httpx>=0.26.0

# Testing
pytest>=7.0.0
