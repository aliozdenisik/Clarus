'use client';

export function OfflineBannerWrapper() {
  // Note: Backend status monitoring removed during Better Auth migration
  // Can be re-implemented with a simple fetch-based health check if needed
  return null;
}
